const CartItem = (props) => {
console.log(props);
  return (
    <>
      {/* <button onClick={() => dispatch(decreaseQuantity(props.product.id))}>
        minus
      </button>
      <p className="text-base text-gray-500 font-semibold">
        {productInCart?.quantity}
      </p>
      <button onClick={() => dispatch(increaseQuantity(props.product.id))}>
        plus
      </button> */}
    </>
  );
};

export default CartItem;
